---
import BaseLayout from '../layouts/BaseLayout.astro';
import ScrollReveal from '../components/ScrollReveal';
import RichText from '../components/RichText.astro';
import TypedTitle from '../components/TypedTitle.astro';
import { getLangFromUrl, t } from '../i18n';

const lang = getLangFromUrl(Astro.url);
const i = t(lang);

const fieldClass =
  'w-full rounded-[8px] border border-black/12 bg-white px-4 py-3.5 font-body text-base text-ink outline-none transition focus:border-amaranth focus:ring-2 focus:ring-amaranth/20';
const labelClass = 'mb-2 block font-body text-xs font-semibold uppercase tracking-[0.22em] text-ink';
const cardClass = 'border border-black/10 bg-paper p-6 shadow-[0_18px_40px_rgba(6,4,3,0.06)] md:p-8';
---

<BaseLayout title={`${i.contact.title} — ${i.hero.name}`} description={i.contact.subtitle}>
  <section class="bg-paper px-6 pt-28 pb-16 text-ink md:pt-32 md:pb-24">
      <div class="site-container grid gap-8 lg:grid-cols-[0.9fr_1.1fr] lg:items-start">
        <ScrollReveal client:visible className="h-full">
          <div class="h-full border border-white/10 bg-ink p-8 text-paper md:p-10">
            <p class="font-body text-xs font-semibold uppercase tracking-[0.35em] text-amaranth-soft">{i.contact.eyebrow}</p>
            <TypedTitle
              as="h1"
              text={i.contact.title}
              trigger="load"
              class="mt-5 max-w-[10ch] font-heading text-4xl leading-none text-paper md:text-6xl"
            />
            <RichText text={i.contact.subtitle} class="mt-6 max-w-xl font-body text-lg leading-8 text-white/74 md:text-xl" />
          </div>
        </ScrollReveal>

        <div class="grid gap-6 xl:grid-cols-2">
          <ScrollReveal client:visible className="h-full">
            <div class={`${cardClass} h-full`}>
              <div class="text-accent-ink mb-6 inline-flex border border-black/10 bg-white px-3 py-2 font-body text-[0.68rem] font-semibold uppercase tracking-[0.26em]">
                UGC
              </div>
              <h2 class="font-heading text-3xl text-ink">{i.contact.ugcForm.title}</h2>

              <form name="ugc-contact" method="POST" data-netlify="true" data-error-msg={i.contact.errorRequired} class="contact-form mt-8 space-y-5">
                <input type="hidden" name="form-name" value="ugc-contact" />

                <div>
                  <label for="ugc-company" class={labelClass}>{i.contact.ugcForm.company}</label>
                  <input id="ugc-company" name="Company" type="text" required class={fieldClass} />
                </div>

                <div>
                  <label for="ugc-email" class={labelClass}>{i.contact.ugcForm.email}</label>
                  <input id="ugc-email" name="Email" type="email" required class={fieldClass} />
                </div>

                <div>
                  <label for="ugc-budget" class={labelClass}>{i.contact.ugcForm.budget}</label>
                  <input id="ugc-budget" name="Budget" type="text" required class={fieldClass} />
                </div>

                <div>
                  <label for="ugc-description" class={labelClass}>{i.contact.ugcForm.description}</label>
                  <textarea id="ugc-description" name="Project Description" rows="6" required class={`${fieldClass} resize-y`}></textarea>
                </div>

                <button
                  type="submit"
                  class="inline-flex w-full items-center justify-center rounded-[8px] bg-ink px-6 py-3 font-body text-xs font-semibold uppercase tracking-[0.26em] text-paper transition hover:bg-amaranth hover:text-ink"
                >
                  {i.contact.send}
                </button>
              </form>

              <div class="contact-success hidden mt-8 border border-green-200 bg-green-50 p-6 text-center">
                <div class="mx-auto mb-3 flex h-12 w-12 items-center justify-center rounded-full bg-green-100">
                  <svg class="h-6 w-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                </div>
                <p class="font-heading text-2xl text-ink">{i.contact.successTitle}</p>
                <p class="mt-2 font-body text-sm text-ink-muted">{i.contact.successMessage}</p>
              </div>
            </div>
          </ScrollReveal>

          <ScrollReveal client:visible className="h-full" delay={0.15}>
            <div class={`${cardClass} h-full bg-white`}>
              <div class="text-accent-ink mb-6 inline-flex border border-black/10 bg-blush-50 px-3 py-2 font-body text-[0.68rem] font-semibold uppercase tracking-[0.26em]">
                {i.contact.seoForm.title}
              </div>
              <h2 class="font-heading text-3xl text-ink">{i.contact.seoForm.title}</h2>

              <form name="seo-contact" method="POST" data-netlify="true" data-error-msg={i.contact.errorRequired} class="contact-form mt-8 space-y-5">
                <input type="hidden" name="form-name" value="seo-contact" />

                <div>
                  <label for="seo-company" class={labelClass}>{i.contact.seoForm.company}</label>
                  <input id="seo-company" name="Company" type="text" required class={fieldClass} />
                </div>

                <div>
                  <label for="seo-email" class={labelClass}>{i.contact.seoForm.email}</label>
                  <input id="seo-email" name="Email" type="email" required class={fieldClass} />
                </div>

                <div>
                  <label for="seo-budget" class={labelClass}>{i.contact.seoForm.budget}</label>
                  <input id="seo-budget" name="Budget" type="text" required class={fieldClass} />
                </div>

                <div>
                  <label for="seo-description" class={labelClass}>{i.contact.seoForm.description}</label>
                  <textarea id="seo-description" name="Work Description" rows="6" required class={`${fieldClass} resize-y`}></textarea>
                </div>

                <button
                  type="submit"
                  class="text-accent-ink inline-flex w-full items-center justify-center rounded-[8px] border border-amaranth bg-paper px-6 py-3 font-body text-xs font-semibold uppercase tracking-[0.26em] transition hover:bg-amaranth hover:text-ink"
                >
                  {i.contact.send}
                </button>
              </form>

              <div class="contact-success hidden mt-8 border border-green-200 bg-green-50 p-6 text-center">
                <div class="mx-auto mb-3 flex h-12 w-12 items-center justify-center rounded-full bg-green-100">
                  <svg class="h-6 w-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                </div>
                <p class="font-heading text-2xl text-ink">{i.contact.successTitle}</p>
                <p class="mt-2 font-body text-sm text-ink-muted">{i.contact.successMessage}</p>
              </div>
            </div>
          </ScrollReveal>
        </div>
      </div>
    </section>
</BaseLayout>

<script>
  import { initContactForms } from '../lib/contactForms.js';

  const initContactPage = () => initContactForms();
  const handlerKey = '__mgInitContactPage';
  const docWithHandler = document as Document & Record<string, (() => void) | undefined>;

  docWithHandler[handlerKey] && document.removeEventListener('astro:page-load', docWithHandler[handlerKey]);
  docWithHandler[handlerKey] = initContactPage;

  initContactPage();
  document.addEventListener('astro:page-load', initContactPage);
</script>
